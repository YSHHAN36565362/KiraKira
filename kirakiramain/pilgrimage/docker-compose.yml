services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: pilgrimage
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pilgrimage-db-data:/var/lib/postgresql/data

volumes:
  pilgrimage-db-data:
